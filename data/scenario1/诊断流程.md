# 场景1：级联故障 - 完整诊断流程

## 一、初始状态

**诊断开始时间**：2026-05-19T10:30:00Z
**当前系统状态**：告警已触发，问题持续中

---

## 二、Plan 阶段（计划生成）

### 2.1 Planner 分析

**Planner 输入**：
- 系统告警：payment-service (critical)、order-service (warning)、api-gateway (warning)
- 任务：诊断系统故障原因

**Planner 输出（生成的计划）**：

```
诊断计划：
1. 获取系统告警概览，了解当前问题的严重程度
2. 获取最近的部署事件，检查是否有最近的代码变更
3. 获取错误日志，了解具体的错误信息
4. 获取异常指标，找出性能瓶颈
5. 获取服务依赖关系，理解服务间的调用关系
6. 获取慢链路，追踪请求的完整路径
7. 综合分析，确定根本原因
8. 生成诊断报告和处理建议
```

**Planner 推理**：
- 多个服务同时出现告警，可能是级联故障
- payment-service 是 critical，可能是根本原因
- 需要通过部署事件和日志来确认

---

## 三、Executor 阶段（执行计划）

### 步骤 1：获取系统告警概览

**工具调用**：`get_alerts()`

**工具返回结果**：
```json
{
  "status": "success",
  "count": 3,
  "data": [
    {
      "timestamp": "2026-05-19T10:19:00Z",
      "event_type": "alert",
      "service": "api-gateway",
      "severity": "warning",
      "message": "Request latency increased"
    },
    {
      "timestamp": "2026-05-19T10:17:00Z",
      "event_type": "alert",
      "service": "order-service",
      "severity": "warning",
      "message": "Request timeout detected"
    },
    {
      "timestamp": "2026-05-19T10:15:00Z",
      "event_type": "alert",
      "service": "payment-service",
      "severity": "critical",
      "message": "Payment service timeout"
    }
  ]
}
```

**LLM 分析**：
```
发现 3 个告警：
- payment-service (critical) 在 10:15 触发
- order-service (warning) 在 10:17 触发（2分钟后）
- api-gateway (warning) 在 10:19 触发（4分钟后）

这表明可能是级联故障：payment-service 故障 → order-service 故障 → api-gateway 故障
```

---

### 步骤 2：获取部署事件

**工具调用**：`get_deployment_events(limit=10)`

**工具返回结果**：
```json
{
  "status": "success",
  "count": 1,
  "data": [
    {
      "timestamp": "2026-05-19T10:15:00Z",
      "event_type": "deployment",
      "service": "payment-service",
      "version": "v2.3.1",
      "status": "success"
    }
  ]
}
```

**LLM 分析**：
```
发现关键信息：
- payment-service 在 10:15:00 部署了 v2.3.1
- 告警也在 10:15:00 触发
- 时间完全吻合！

推论：新版本部署后立即出现故障，很可能是部署 Bug 导致的
```

---

### 步骤 3：获取错误日志

**工具调用**：`get_error_logs(limit=10)`

**工具返回结果**：
```json
{
  "status": "success",
  "count": 5,
  "data": [
    {
      "timestamp": "2026-05-19T10:16:00Z",
      "service": "api-gateway",
      "level": "ERROR",
      "message": "Payment service timeout",
      "trace_id": "trace_001",
      "status": "error"
    },
    {
      "timestamp": "2026-05-19T10:16:00Z",
      "service": "order-service",
      "level": "ERROR",
      "message": "Payment service timeout",
      "trace_id": "trace_001",
      "status": "error"
    },
    {
      "timestamp": "2026-05-19T10:16:00Z",
      "service": "payment-service",
      "level": "ERROR",
      "message": "Payment service timeout",
      "trace_id": "trace_001",
      "status": "error"
    }
  ]
}
```

**LLM 分析**：
```
关键发现：
- 所有错误都是 "Payment service timeout"
- 三个服务都在同一时间（10:16:00）出现错误
- 使用相同的 trace_id (trace_001)，说明是同一个请求链路

这证实了级联故障的假设：
payment-service 超时 → order-service 等待超时 → api-gateway 等待超时
```

---

### 步骤 4：获取异常指标

**工具调用**：`get_metrics_anomalies(threshold_percentile=0.8)`

**工具返回结果**：
```json
{
  "status": "success",
  "threshold_percentile": 0.8,
  "anomalies": {
    "request_latency_ms": {
      "threshold": 1000,
      "anomaly_points": [
        {
          "timestamp": "2026-05-19T10:16:00Z",
          "value": 5000
        },
        {
          "timestamp": "2026-05-19T10:17:00Z",
          "value": 5100
        },
        {
          "timestamp": "2026-05-19T10:18:00Z",
          "value": 5200
        }
      ]
    },
    "error_rate_percent": {
      "threshold": 5,
      "anomaly_points": [
        {
          "timestamp": "2026-05-19T10:16:00Z",
          "value": 15
        }
      ]
    }
  }
}
```

**LLM 分析**：
```
指标异常确认：
- request_latency_ms: 从正常的 100ms → 异常的 5000ms（50倍增长！）
- error_rate_percent: 从正常的 0.1% → 异常的 15%（150倍增长！）

这些数据强烈支持"级联故障"的诊断
```

---

### 步骤 5：获取服务依赖关系

**工具调用**：`get_service_dependencies(service="api-gateway")`

**工具返回结果**：
```json
{
  "status": "success",
  "service": "api-gateway",
  "dependencies": ["user-service", "order-service", "payment-service"],
  "count": 3
}
```

**LLM 分析**：
```
服务依赖关系：
- api-gateway 依赖 payment-service
- order-service 也依赖 payment-service（通过 api-gateway 的调用）

这解释了为什么 payment-service 的故障会级联到其他服务
```

---

### 步骤 6：获取慢链路

**工具调用**：`get_slow_traces(threshold_ms=1000, limit=5)`

**工具返回结果**：
```json
{
  "status": "success",
  "threshold_ms": 1000,
  "count": 3,
  "data": [
    {
      "trace_id": "trace_001",
      "start_time": "2026-05-19T10:16:00Z",
      "total_duration_ms": 5000,
      "status": "error",
      "spans": [...]
    },
    {
      "trace_id": "trace_002",
      "start_time": "2026-05-19T10:17:00Z",
      "total_duration_ms": 5100,
      "status": "error",
      "spans": [...]
    },
    {
      "trace_id": "trace_003",
      "start_time": "2026-05-19T10:18:00Z",
      "total_duration_ms": 5200,
      "status": "error",
      "spans": [...]
    }
  ]
}
```

**LLM 分析**：
```
慢链路分析：
- 发现 3 条超过 1000ms 的链路
- 所有链路都在 10:16-10:18 之间
- 所有链路都是 error 状态

这与之前的告警时间完全吻合
```

---

### 步骤 7：获取链路详情

**工具调用**：`get_trace_details(trace_id="trace_001")`

**工具返回结果**：
```json
{
  "status": "success",
  "trace_id": "trace_001",
  "total_duration_ms": 5000,
  "status_code": "error",
  "spans_count": 3,
  "data": {
    "trace_id": "trace_001",
    "start_time": "2026-05-19T10:16:00Z",
    "end_time": "2026-05-19T10:16:05Z",
    "total_duration_ms": 5000,
    "status": "error",
    "spans": [
      {
        "span_id": "span_1",
        "service": "api-gateway",
        "operation": "POST /order",
        "duration_ms": 1667,
        "status": "error",
        "error_message": "Payment service timeout"
      },
      {
        "span_id": "span_2",
        "service": "order-service",
        "operation": "createOrder",
        "duration_ms": 1667,
        "status": "error",
        "error_message": "Payment service timeout"
      },
      {
        "span_id": "span_3",
        "service": "payment-service",
        "operation": "processPayment",
        "duration_ms": 1667,
        "status": "error",
        "error_message": "Payment service timeout"
      }
    ]
  }
}
```

**LLM 分析**：
```
链路追踪详情：
- 请求从 api-gateway 开始
- 依次调用 order-service 和 payment-service
- payment-service 耗时最长（1667ms）
- 所有服务都返回 "Payment service timeout" 错误

这清楚地显示了故障的传播路径：
api-gateway → order-service → payment-service（故障点）
```

---

## 四、Replan 阶段（重新规划）

### 4.1 综合分析

**已收集的证据**：
1. ✓ 部署事件：payment-service 在 10:15 部署 v2.3.1
2. ✓ 告警时间：payment-service 在 10:15 触发 critical 告警
3. ✓ 错误日志：所有服务都有 "Payment service timeout" 错误
4. ✓ 异常指标：latency 增加 50 倍，error_rate 增加 150 倍
5. ✓ 服务依赖：payment-service 是关键依赖
6. ✓ 链路追踪：payment-service 是故障点

**诊断结论**：
```
根本原因：payment-service v2.3.1 部署 Bug
问题表现：payment-service 处理支付请求超时
级联影响：order-service 和 api-gateway 都无法完成请求
```

### 4.2 Replan 决策

**是否需要重新规划**：否

**理由**：
- 已经收集了充分的证据
- 根本原因已经明确
- 不需要进一步的诊断

---

## 五、最终诊断报告

### 5.1 问题描述

payment-service 在 10:15 部署新版本 v2.3.1 后，立即出现支付超时问题。由于 order-service 和 api-gateway 都依赖 payment-service，导致级联故障，影响整个订单处理流程。

### 5.2 问题链条

```
部署 v2.3.1（10:15:00）
  ↓
payment-service 支付超时（10:15:30）
  ↓
order-service 无法完成订单（10:17:00）
  ↓
api-gateway 请求堆积（10:19:00）
  ↓
错误率上升至 15%（10:20:00）
```

### 5.3 关键证据

**部署事件**：
- payment-service v2.3.1 在 10:15:00 部署

**错误日志**：
- "Payment service timeout" 在 10:16:00 开始出现
- 影响 api-gateway、order-service、payment-service

**异常指标**：
| 指标名称 | 异常值 | 正常值 | 异常时间 |
|---------|-------|-------|---------|
| request_latency_ms | 5000ms | 100ms | 10:16:00 |
| error_rate_percent | 15% | 0.1% | 10:16:00 |

**链路追踪**：
- trace_001: api-gateway → order-service → payment-service (5000ms, error)

### 5.4 处理建议

**立即处理（当前）**：
1. 回滚 payment-service 到 v2.3.0
2. 监控系统恢复情况

**短期处理（24小时内）**：
1. 分析 v2.3.1 的代码变更，找出导致超时的 Bug
2. 修复 Bug 并进行充分测试
3. 重新部署修复后的版本

**长期优化（本周内）**：
1. 加强部署前的性能测试
2. 实施灰度发布策略，避免全量部署导致的级联故障
3. 优化 payment-service 的超时配置

### 5.5 风险评估

| 评估项 | 结果 |
|-------|------|
| 当前风险等级 | 高 |
| 受影响服务 | payment-service、order-service、api-gateway |
| 影响范围 | 所有订单处理请求 |
| 是否已恢复 | 否 |
| 建议处理优先级 | 立即 |

---

## 六、诊断过程摘要

| 步骤 | 工具调用 | 关键发现 |
|-----|---------|---------|
| 1 | `get_alerts()` | 发现 3 个告警，payment-service 是 critical |
| 2 | `get_deployment_events()` | payment-service 在 10:15 部署 v2.3.1 |
| 3 | `get_error_logs()` | 所有服务都有 "Payment service timeout" 错误 |
| 4 | `get_metrics_anomalies()` | latency 增加 50 倍，error_rate 增加 150 倍 |
| 5 | `get_service_dependencies()` | payment-service 是关键依赖 |
| 6 | `get_slow_traces()` | 发现 3 条超过 1000ms 的链路 |
| 7 | `get_trace_details()` | payment-service 是故障点 |

---

## 七、诊断统计

- **诊断开始时间**：2026-05-19T10:30:00Z
- **诊断结束时间**：2026-05-19T10:30:45Z
- **诊断耗时**：45 秒
- **执行步骤数**：7 步
- **诊断状态**：正常完成
- **根本原因确定**：是
- **建议生成**：是
