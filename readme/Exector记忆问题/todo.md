1. ParameterPool字段是否全面
2. 如何能让llm输出严谨的字段 eg：payment-service
3. 对于上下文 past_steps 可能会很长，如何精简（前面的取摘要、后面的几行保留完整的）有必要吗？

锚点参数
动态推理参数 通过past_steps注入


对于一个工具调用，如果参数可以根据前面的数据或者会话得到，那不会有什么问题，但是如果前面没有参数的线索呢？比如说，计划不合理，需要replanner重新规划呢？这时候参数该怎么设置？


对于Executor、Replanner 二者的任务不一样
```
Executor作为执行（锚点+精确量+摘要？需要近几轮的完整信息吗）
Replanner作为决策（锚点+精确量+摘要+近几轮的完整信息）
两者的提示词、注入的信息应该不同

```